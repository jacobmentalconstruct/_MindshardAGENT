## Local Notes
- These prompt docs are editable defaults for MindshardAGENT behavior.
- Project-specific overrides may replace same-named files from `.mindshard/state/prompt_overrides/`.
- Extra override files are appended after the standard ordered set.
