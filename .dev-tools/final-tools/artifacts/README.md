# Artifacts Folder

This folder is reserved for generated outputs that future tools can write and later tools can consume.

Examples:

- codebase indexes
- dependency graphs
- docs catalogs
- config maps
- test maps
- change impact reports

Keep generated files out of the tool source folders.
