# Drop Bin

Place rough scripts here when they need to be converted into stable `_app-journal` tools.

Promotion rule:

- move logic into `lib/` if it is shared
- expose stable operations through `tools/`
- keep the UI on top of the shared store layer
