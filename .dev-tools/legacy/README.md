# Legacy

This folder contains older loose scripts that are preserved for reference.

## Default Rule

Agents should not start here unless the user explicitly asks about a legacy script or asks to convert one into a real package tool.

## Current Contents

- `root-scripts/`
  - historical standalone scripts from the old flat `.dev-tools` layout
